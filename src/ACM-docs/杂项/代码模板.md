# 代码模板

## 本地文件操作

```cpp
#include<bits/stdc++.h>
#define ll long long
#define ptn putchar('\n')
#define _LOCAL_
#ifdef _LOCAL_
#include <windows.h>
#endif
using namespace std;

inline ll read() {ll x = 0, z = 1;char c = getchar();while (!isdigit(c)) {if (c == '-')z = -1;c = getchar();}while (isdigit(c)) {x = (x << 1) + (x << 3) + (c ^ 48);c = getchar();}return z * x;}
template<typename T>
inline void writ(T x,const char &c = -1){if(x<0) {putchar('-');x=(~x)+1;}if(x>9)writ(x/10);putchar(x-x/10*10+48);if(~c)putchar(c);}




void ClearDewy(){



}



int main()
{
#ifdef _LOCAL_
    freopen("data.in", "r", stdin);
    freopen("data.out", "w", stdout);
    LARGE_INTEGER litmp, start, finish;
    QueryPerformanceFrequency(&litmp);
    QueryPerformanceCounter(&start);
#endif
    //cin.tie(nullptr)->sync_with_stdio(false);

    //int T=read();while (T--)
    Qingtuan();

#ifdef _LOCAL_
    QueryPerformanceCounter(&finish);
    freopen("CON", "r", stdin);
    freopen("CON", "w", stdout);
    printf("%.2f\n", (double)(finish.QuadPart - start.QuadPart) / (double)litmp.QuadPart * 1000);
#endif
    return 0;
}
```



