# 三维计算几何

## 点积

```cpp
template <typename T>
T Dot(Vector p,Vector q){
    return p.x*q.x+p.y*q.y+p.z*q.z;
}
```

## 叉积

-   几何意义为平面的法向量

```cpp
Vector Cross(Vector p,Vector q){
    return Vector(p.y*q.z-q.y*p.z,p.z*q.x-q.z*p.z,p.x*q.y-q.x*p.y);
}
```

