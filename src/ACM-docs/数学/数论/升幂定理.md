# 升幂定理

## 定义：

升幂定理分为两部分，模为奇素数与模为$2$，简记为$LTEp$和$LTE2$。记$v_p(n)$为素数$p$在整数 中的个数，即$p^{v_p(n)}$恰好整除整数$n$，$p^{v_p(n)+1}$不整除整数 。

## 模为奇素数

$$
v_p(a^n-b^n)=v_p(a-b)+v_p(n)
$$

## 模为2

$$
\begin{cases}
v_2(a^n-b^n)&=v_2(a-b) &(n为奇数)\\
v_2(a^n-b^n)&=v_2(a-b)+v_2(a+b)+v_2(n)-1&(n为偶数)
\end{cases}
$$

### 