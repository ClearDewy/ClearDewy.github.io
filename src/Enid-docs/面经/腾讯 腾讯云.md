# 腾讯 腾讯云

3.5

算法

1、判断镜像二叉树（lgn和n前者效率一定比后者高吗）

2、求二叉树中最远两个节点之间的距离

提问：

1、

```c++
int a[1001][1001];
for(int i=0;i<1001;i++){
	for(int j=0;j<1001;i++){
		sum+=a[i][j];
	}
}

for(int i=0;i<1001;i++){
	for(int j=0;j<1001;i++){
		sum+=a[j][i];
	}
}
```

这两个循环哪个效率高，为什么？

2、现代操作系统的内存和页表是怎么实现的

3、知道newsql吗？（不知道）

4、mysql的事务都是怎么实现的

5、锁是怎么实现的

